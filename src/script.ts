// selecting our elements
const numberWrap = document.getElementsByClassName("number-Wrap")[0];
const numberText = document.getElementsByClassName("number-Text")[0];
const replayBtn = document.getElementsByClassName("replay-Btn")[0];

// functions

// event lisnters
